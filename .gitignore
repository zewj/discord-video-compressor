node_modules/
dist/
build/probe*
build/source.png
# Multi-size build artifacts (icon_16.png .. icon_128.png, etc.) — only
# the final icon.ico and the Linux icon-256.png are committed.
build/icon_*.png
*.log
.DS_Store
Thumbs.db

# Local Claude Code settings (machine-specific permissions/paths)
.claude/settings.local.json

# Copyrighted character art for the Akeno / Astolfo themes — kept local, not
# redistributed. Drop your own akeno.png / astolfo.png into renderer/assets/.
renderer/assets/*.png
